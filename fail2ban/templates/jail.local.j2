# {{ ansible_managed }}

[DEFAULT]
chain = fail2ban
{% for jail, config in (fail2ban__jail_config or {}).items() %}

[{{ jail }}]
{% for key, value in (config or {}).items() %}
{{ key }} = {{ value }}
{% endfor %}
{% endfor %}
